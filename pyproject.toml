[project]
name = "duck-ai"
version = "1.1.0"
description = "Duck AI - Deep Reinforcement Learning"
requires-python = ">=3.14"
license = { file = "LICENSE" }

[build-system]
requires = ["setuptools==78.1.1"]
build-backend = "setuptools.backends.legacy:build"

[tool.setuptools.packages.find]
where = ["src"]
